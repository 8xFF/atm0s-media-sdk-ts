export interface MicrophoneShareProps {
  source_name: string;
  track_name: string;
}

export function MicrophoneShare({
  source_name,
  track_name,
}: MicrophoneShareProps) {
  return <div></div>;
}
